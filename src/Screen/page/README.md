# Screen Pages

Page classes in this folder must use the same names as pages in ScreenUI (EEZ Studio).

Current mapping:
- `Load` page -> `Load` class (`main/load.h`)
- `Main` page -> `Main` class (`main/main.h`)
- `Info` page -> `Info` class (`main/info.h`)
